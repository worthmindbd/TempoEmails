---
import Header from '../components/sections/Header.astro';
import Footer from '../components/sections/Footer.astro';
import ToastContainer from '../components/ui/ToastContainer.astro';
import CookieConsent from '../components/ui/CookieConsent.astro';
import { SITE_CONFIG } from '../lib/config';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  keywords?: string;
  ogImage?: string;
  ogType?: string;
}

const {
  title = 'TempoEmails — Free Disposable Temporary Email & Verification Codes',
  description = 'Get a free, instant temporary disposable email address to sign up on any website. Auto-extracts verification codes and OTPs without exposing your real inbox. 100% anonymous, zero logs.',
  keywords = 'temp mail, temporary email, disposable email, throwaway email, burner email, fake email generator, 10 minute mail, 24 hour temp mail, temp mail for discord, temporary email for chatgpt, temp mail for telegram, temporary mail for steam, disposable email for facebook, disposable mail rest api, free temporary inbox, anonymous temp email no log',
  ogImage = '/og-image.png',
  ogType = 'website',
} = Astro.props;

const siteUrl = Astro.site?.origin || SITE_CONFIG.siteUrl;
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
const ogImageURL = new URL(ogImage, siteUrl);

// Google Analytics 4 & AdSense
const gaId = import.meta.env.PUBLIC_GOOGLE_ANALYTICS_ID;
const adsenseId = import.meta.env.PUBLIC_GOOGLE_ADSENSE_ID;

// JSON-LD Structured Data for WebSite, WebApplication, and Organization
const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'TempoEmails',
  url: canonicalURL.origin,
  description: 'Instant, anonymous disposable email addresses with automatic OTP verification code extraction.',
  potentialAction: {
    '@type': 'SearchAction',
    target: `${canonicalURL.origin}/blog?q={search_term_string}`,
    'query-input': 'required name=search_term_string',
  },
};

const webAppSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'TempoEmails',
  url: canonicalURL.origin,
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'All',
  browserRequirements: 'Requires JavaScript. Requires HTML5.',
  offers: {
    '@type': 'Offer',
    price: '0.00',
    priceCurrency: 'USD',
  },
  featureList: [
    'Instant disposable email address generation',
    'Automated OTP verification code extraction',
    'Custom username and domain aliases',
    '10-minute to 30-day extended lifespan retention',
    'QR code desktop-to-mobile handoff',
    'Zero database logging and total anonymity',
    'Multi-inbox session switching',
  ],
};

const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'TempoEmails',
  url: canonicalURL.origin,
  logo: new URL('/favicon.svg', canonicalURL.origin).href,
  sameAs: [
    'https://github.com/TempoEmails',
  ],
};
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- SEO Meta Tags -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content={keywords} />
    <meta name="author" content="TempoEmails" />
    <meta name="publisher" content="TempoEmails" />
    <meta name="theme-color" content="#ea2804" />
    <meta name="application-name" content="TempoEmails" />
    <meta name="apple-mobile-web-app-title" content="TempoEmails" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

    <!-- Search Engine Indexing Directives -->
    <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
    <meta name="googlebot" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
    <meta name="bingbot" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
    <link rel="canonical" href={canonicalURL.href} />
    <link rel="sitemap" type="application/xml" title="Sitemap Index" href="/sitemap-index.xml" />
    <link rel="sitemap" type="application/xml" title="Sitemap" href="/sitemap.xml" />

    <!-- Google Analytics 4 (gtag.js) -->
    {gaId && (
      <>
        <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${gaId}`} />
        <script is:inline define:vars={{ gaId }}>
          window.dataLayer = window.dataLayer || [];
          function gtag() { dataLayer.push(arguments); }
          gtag('js', new Date());
          gtag('config', gaId, { page_path: window.location.pathname });
        </script>
      </>
    )}

    <!-- Google AdSense -->
    {adsenseId && (
      <script
        is:inline
        async
        src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${adsenseId}`}
        crossorigin="anonymous"
      />
    )}

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(webAppSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />

    <!-- Google Fonts: Bricolage Grotesque (Display), Inter (Body), JetBrains Mono (Code) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,600;700;800&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Zero-FOUC Theme Script -->
    <script is:inline>
      (function () {
        const storedTheme = localStorage.getItem('theme');
        const userPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (storedTheme === 'dark' || (!storedTheme && userPrefersDark)) {
          document.documentElement.classList.add('dark');
        } else {
          document.documentElement.classList.remove('dark');
        }
      })();
    </script>
  </head>
  <body class="bg-[#f9f7f3] dark:bg-[#0e0e11] text-[#202020] dark:text-[#fcfcfc] min-h-screen flex flex-col antialiased selection:bg-[#ea2804] selection:text-white">
    <!-- Atmospheric Ambient Glow Mesh Background -->
    <div class="fixed inset-0 hero-glow-mesh pointer-events-none z-0"></div>

    <Header />

    <main class="flex-1 relative z-10">
      <slot />
    </main>

    <Footer />

    <ToastContainer />
    <CookieConsent />
  </body>
</html>
